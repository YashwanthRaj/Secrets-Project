# Secrets-Project
Client Side website which uses the Secrets API in order to reveal secrets anonymously, like Whisper app. The project uses a public Secrets API to retrieve and display random anonymous secrets in real-time using Axios and Express.js.  
